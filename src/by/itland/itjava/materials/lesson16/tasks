Задание1. Создайте класс Task1 с приватным полем оценок учеников (ArrayList)
    - Заполнить его из метода main случаными оценками [1,10].
    - Удалить неудовлетворительные оценки [1,3] из списка, описать для этого метод
    clearBad(List<Integer> grades)
    - Вывести на консоль информацию до удаления и после удаления

Задание2. В main класса Task2 определить два множества (set1, set2) на основе целых чисел
    Создать статические методы для определения пересечения множеств
    getCross(Set<Integer> set1, Set<Integer> set2) и
    объединения множеств getUnion(Set<Integer> set1, Set<Integer> set2)
    Можно использовать методы из Set.

Задание3. Создайте класс Task3, в методе main реализовать ввод чисел до тех пор пока пользователь
    не введет слово "end". Записать все значея в список, но так чтобы, вначале списка были записаны
    отрицательные элементы, а потом положительные, при этом должен сохраниться их вводимый порядок

Задание4. Создайте класс Task4, в методе в методе main реализовать ввод слов до тех пор пока
    пользователь не введет слово "end", реализовать подсчет встречаемости слов в формате слово=количество
    Слова с разными регистрами букв считаются разными словами. PS Можете текст из поэмы, и посчитать слова

Задание5. Создать класс Task5, реализовать в методе main считалочку по-кругу.
    Дано: в кругу стоят N человек, вводит пользователь, алгоритм уничтожает каждого второго
    Вывести: номер игрока, оставшегося в живых
    Это нужно проделать используя ArrayList и LinkedList, узнать какая коллекция быстрее.



